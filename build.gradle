group = "${artifactGroup}"
version = "${libraryVersion}"

allprojects {
    repositories {
        mavenLocal()
        mavenCentral()
    }
}

subprojects {
    if (name != 'samples')
        apply plugin: 'maven-publish'
}